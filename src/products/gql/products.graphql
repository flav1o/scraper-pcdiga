type Product {
  _id: ID!
  ean: String!
  name: String!
  url: String!
  prices: [ProductPrice]
}

type ProductPrice {
  _id: ID!
  currentPrice: Float!
  originalPrice: Float!
  priceDifference: Float!
  isOnDiscount: Boolean
  discountPercentage: Float!
}

input CreateProductInput {
  ean: String!
  name: String!
  url: String!
}

input UpdateProductInput {
  name: String
  url: String
}

input CreateProductPriceInput {
  currentPrice: Float!
  originalPrice: Float!
  priceDifference: Float!
  isOnDiscount: Boolean
  discountPercentage: Float!
}

type Query {
  getProduct(url: String!): Product!
}